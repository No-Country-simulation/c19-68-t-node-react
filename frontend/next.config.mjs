/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["i.pravatar.cc", "res.cloudinary.com", "desarrolloweb.com","example.com" ],
  },
};

export default nextConfig;
